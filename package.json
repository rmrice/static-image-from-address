{
  "name": "static-image-from-address",
  "version": "1.0.0",
  "description": "Get a static image from an address",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mapbox",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2"
  }
}
